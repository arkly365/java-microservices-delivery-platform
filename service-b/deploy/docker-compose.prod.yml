services:
  service-b-prod:
    image: 127.0.0.1:5000/service-b:prod-${IMAGE_TAG:-latest}
    container_name: service-b-prod
    ports:
      - "127.0.0.1:28082:8080"
    environment:
      APP_ENV: prod
      APP_BRANCH: ${APP_BRANCH}
      APP_IMAGE_TAG: ${IMAGE_TAG:-latest}
    restart: unless-stopped
    networks:
      - lab-net

networks:
  lab-net:
    external: true
    name: jenkins-cicd-lab_lab-net