services:
  sample-java-app-prod:
    image: localhost:5000/sample-java-app:${IMAGE_TAG}
    container_name: sample-java-app-prod
    ports:
      - "8082:8080"
    environment:
      APP_ENV: prod
      APP_BRANCH: ${APP_BRANCH}
      APP_IMAGE_TAG: ${IMAGE_TAG}
    restart: unless-stopped